/**
 * @param {{ variant?: 'primary'|'secondary'|'ghost', children: React.ReactNode }} props
 */
export function Button({ variant = 'primary', children, ...props }) {
  return null; // TODO: Implement in Phase 2
}
